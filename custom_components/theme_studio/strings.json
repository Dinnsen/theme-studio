{
  "config": {
    "step": {
      "user": {
        "title": "Theme Studio",
        "description": "Install Theme Studio services and asset bootstrapper.",
        "data": {
          "packages_path": "Packages path",
          "lovelace_path": "Lovelace YAML path",
          "theme_studio_path": "Theme Studio data path",
          "themes_path": "Themes output path",
          "overwrite": "Overwrite existing managed files",
          "backup": "Create backups before overwriting"
        }
      }
    },
    "error": {
      "single_instance_allowed": "Only one Theme Studio instance is allowed."
    },
    "abort": {
      "single_instance_allowed": "Theme Studio is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Theme Studio options",
        "description": "Control how Theme Studio reinstalls managed assets.",
        "data": {
          "overwrite": "Overwrite existing managed files",
          "backup": "Create backups before overwriting"
        }
      }
    }
  }
}
